# MariaDB deployment assets

`create_vibmail_app_database.sql.template` is a template. Replace only the password placeholder in a protected temporary copy, execute it through the local MariaDB root socket, and securely delete the temporary copy. Do not alter the existing `vibmail` schema.
