# MailStack Permanent Project Memory

This document serves as the persistent AI memory for the MailStack project. It must be read at the start of any session and updated immediately when significant architectural, feature, or workflow changes occur.

## Tech Stack & Dependencies
*   **Operating System:** Designed for Ubuntu 24.04+ (deployment via bash scripts).
*   **Mail Transfer Agent (MTA):** Postfix (handles SMTP and internal delivery via LMTP).
*   **IMAP/POP3 Server:** Dovecot (handles LMTP from Postfix, and provides IMAP/POP3 access to users).
*   **Mail Authentication:** OpenDKIM (generates and verifies DKIM signatures for outbound/inbound mail).
*   **Database:** MariaDB (stores mail domains, mailboxes, aliases, and application data).
*   **Backend Application:** Django (Python 3.12+). Runs via Gunicorn.
*   **Web Server / Reverse Proxy:** Nginx (serves public static content, Django static/attachment files, and proxies Gunicorn).
*   **SSL/TLS:** Let's Encrypt / Certbot (for both Nginx HTTPS and Postfix/Dovecot TLS).
*   **Frontend (App):** HTML templates via Django, Bootstrap/custom CSS.

## Core Architecture & File Organization
*   **`install.sh`**: The primary single-node deployment script. It provisions users (`vmail`), packages, databases, and configures the entire stack natively without Docker.
*   **`deployment/templates/`**: Configuration templates for services (Nginx, Postfix, Dovecot, Systemd). These templates use `{{VAR}}` syntax, rendered dynamically by `scripts/render_template.py`.
*   **`mailbox-app/`**: The Django application.
    *   **`config/`**: Django settings (e.g., `production.py`).
    *   **`apps/ingestion/`**: Handles email parsing/processing logic.
    *   **`manage.py`**: Django management script.
*   **`public-site/`**: Contains static assets for the public-facing landing page/documentation.
*   **`/var/vmail/`**: Target directory on deployed servers where Dovecot stores maildir format emails.
*   **`/run/vibmail/`**: Runtime directory on deployed servers containing the Gunicorn socket.

## Current Active Features and Workflow
*   **System Users (Dynamic):** The deployment creates a system user `vmail`. Its dynamic UID/GID are resolved and injected into Postfix/Dovecot configurations to prevent permission mismatches (`first_valid_uid=10`, `static:$VMAIL_UID`).
*   **Gunicorn Socket Permissions:** Nginx accesses Gunicorn via `/run/vibmail/gunicorn.sock`. Gunicorn runs under systemd with `UMask=0007` so Nginx (`www-data` group) can access the socket.
*   **OpenDKIM Integration:** OpenDKIM automatically generates 2048-bit RSA keys during `install.sh` and links with Postfix as a milter to sign outbound mail securely.
*   **Development Workflow:** Any template updates (like Nginx `app.conf.tpl` or `99-vibmail.conf.tpl`) must be validated against `install.sh` to ensure correct rendering.
*   **Auto-Update System:** Implemented via GitHub Actions (`.github/workflows/auto_release.yml`) and a Dashboard UI page. Administrators can trigger an upgrade, which polls GitHub for the latest release, downloads the source/checksum zip files, and spawns the `sudo /opt/vibmail/app/scripts/upgrade.sh` script via a `sudoers.d` rule allowing the `vmail` user passwordless execution of the upgrade script. `upgrade.sh` reports JSON progress to `/tmp/vibmail_update_status.json`, polled by the frontend via AJAX.
*   **Design System & Typography (Licora Migration — v1.3.4-rc.3+):** The UI design system is now fully aligned with the Licora project's structural tokens. Font family is `Inter` (loaded via Google Fonts — added to `base.html`). Design tokens are defined in `foundation.css` using `--ui-*` variables: `--ui-font-size-base: 0.8125rem (13px)`, `--ui-font-size-sm: 0.75rem (12px)`, `--ui-font-size-xs: 0.6875rem (11px)`, border-radii `--ui-radius-sm: 6px / --ui-radius-md: 8px / --ui-radius-lg: 12px`, control heights `--ui-control-height-sm: 26px / --ui-control-height-md: 30px / --ui-control-height: 32px (frozen CI)`, card padding `y:10px / x:12px`, topbar `44px`. All component styles in `app.css` and `foundation.css` consume these tokens. The sidebar navigation was compacted (32px item height, left-border active states) matching the Licora app-shell precisely. **Form design (2026-09-17):** Inputs/selects use `min-height:30px`, `padding: 4px 8px`, `border: var(--ui-border)`, `border-radius: var(--ui-radius-sm)`, `font-size:12px`. Focus ring uses `box-shadow: 0 0 0 3px rgba(11,79,245,0.14)`. Labels use `margin-bottom:4px`, `font-weight:500`. Help-text `font-size:11px`. Errors `padding:7px 10px`, `border-radius:6px`. form-group `margin-bottom:14px`. Buttons `padding:4px 8px`, `border-radius:6px`. Alerts `padding:9px 11px`, `font-size:11px`. auth-card `padding:22px`, `box-shadow:none`. **Card & Section design (2026-09-17):** `.panel` layout structure was completely replaced by Licora's `.card` structure (`.card`, `.card-header`, `.card-body`, `.card-footer`). `card-header` uses `background: #fbfcfe`, `border-bottom: 1px solid var(--ui-border)`. `card-body` uses compact `12px` padding globally, removing the 16px responsive padding. All `<section class="panel">` elements migrated to `.card` with respective `.card-body` content wrappers. **Page-header cleanup (2026-09-17):** All `.eyebrow` sub-titles and `.muted` description paragraphs removed from every `.page-header` across all 8 templates (dashboard, mailboxes/list, mailboxes/create, mailboxes/confirm_delete, messages/confirm_delete, accounts/user_list, accounts/user_form, accounts/user_confirm_delete). Each page-header now contains only `<h1>` and the optional primary action button — clean, compact, and production-ready. **Table design (2026-09-17):** `th, td` use `padding: 6px 12px`, `th` uses `font-size: 11px`, `line-height: 1.25`, `font-weight: 600`, `text-transform: none`, `white-space: nowrap`, `color: var(--ui-text-muted)`, `background: #f7f9fc`. `td` uses `font-size: 12px`, `color: var(--ui-text)`. Added `tbody tr:hover { background: #f7faff; }`. `app.css` MUST be loaded *before* `foundation.css` as enforced by CI contract. **Updater page & popup log modal (2026-09-17):** `system_update.html` was fully redesigned using Licora's updater grid layout. The page now has three sections: a two-column `licora-updater-grid` (Current Installation + Latest Stable Release cards), an Installation State card with an active job progress bar, and a terminal-style `#licora-update-log-modal` popup. All CSS for `.licora-updater-card`, `.licora-updater-grid`, `.vib-modal-backdrop`, `.vib-log-viewport`, `.vib-log-row`, `.vib-log-badge`, `.licora-active-job`, `.licora-job-progress` was appended to `app.css` from Licora's `updater.css`. Licora-specific CSS variables (like `--border-card`, `--text-primary`) were exhaustively mapped and replaced with their MailStack equivalents (like `--ui-border`, `--ui-text`) to preserve visual integrity. Log color tokens (`--log-bg`, `--log-info-bg`, `--log-info-text`, `--log-warning-*`, `--log-success-*`, `--log-debug-*`) were added to `foundation.css :root`. The JS polls `/dashboard/update_status` and maps response `step`/`message` to `info`/`error` log rows appended into the `.vib-log-viewport`. 8/8 UI tests pass. Forensic audit: PASS (BLOCKING_FINDINGS=0).
*   **Template HTML Structure:** The `<!doctype html>` declaration must be on line 1 as the very first token to prevent Quirks Mode and premature document body initialization in HTML parsers. Template directives inside `<head>` like `{% load static %}` in `templates/base.html` must be wrapped in an HTML comment (`<!--{% load static %}-->`) after the `<meta>` and `<title>` elements. This allows Django to execute the tag while preventing HTML parsers/linters from interpreting raw template curly braces as character tokens that prematurely close `<head>` and cause `<meta charset>` or `<meta name="viewport">` to be flagged as being in `<body>`.
*   **Active Release Identity:** Maintained as `1.3.4-rc.3` (release tag `v1.3.4-rc.3`), following standard SemVer 2.0 (`MAJOR.MINOR.PATCH-rc.N`).
*   **CI Workflow & Forensic Inventory Sync:** Whenever any repository file is modified, `python scripts/generate_inventory.py --root .` must be executed to refresh `docs/FORENSIC_FILE_INVENTORY.json`, and `python scripts/forensic_audit.py --root .` must be run to verify zero blocking findings prior to finishing tasks.
---

> **Note to AI Agents:** Review this file when joining a context to understand the project architecture, particularly the specific permission structures (like `vmail` and OpenDKIM groups) established in the MailStack.
